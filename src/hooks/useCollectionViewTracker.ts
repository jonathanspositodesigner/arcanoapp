// Collection view tracking removed - no longer needed
export const trackCollectionView = async (
  collectionId: string,
  collectionSlug: string,
  collectionName: string
) => {
  // Tracking disabled - function kept for backwards compatibility
  return;
};
