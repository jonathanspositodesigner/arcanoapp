

# Atualizar Chave API do Google Gemini

## Situação
Você já tem a chave nova: `AIzaSyCClEY2LEZQacs5pi2-all1Mk6wbtgVHfI`
O projeto já tem o secret `GOOGLE_GEMINI_API_KEY` configurado e a edge function `admin-update-google-key` pronta.

## Plano
1. **Atualizar o secret `GOOGLE_GEMINI_API_KEY`** com a nova chave via ferramenta de secrets (preciso sair do modo planejamento para usar a ferramenta de adicionar/atualizar secret)
2. **Testar** gerando uma imagem na página gerar-imagem para confirmar que a chave funciona

## O que preciso fazer
Aprovar este plano para eu poder executar a atualização do secret diretamente com a nova chave que você forneceu.

Nenhuma alteração de código é necessária — apenas trocar o valor do secret.

